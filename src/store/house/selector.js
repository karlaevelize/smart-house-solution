export const selectLamps = (reduxState) => reduxState.house.lamps

export const selectRadio = (reduxState) => reduxState.house.radio

export const selectThermostat = (reduxState) => reduxState.house.thermostat

export const selectConsumption = (reduxState) => reduxState.house.consumption

export const selectState = (reduxState) => reduxState.house
